import { Loader2, X } from 'lucide-react';
import type { CodFeeFormState, CodWaybill } from '../types';
interface Props { waybill: CodWaybill | null; formState: CodFeeFormState; isSubmitting: boolean; error: string; onChange: (value: string) => void; onClose: () => void; onSubmit: () => void }
export default function UpdateCodFeeDialog({ waybill, formState, isSubmitting, error, onChange, onClose, onSubmit }: Props) {
  if (!waybill) return null;
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4"><div className="w-full max-w-md overflow-hidden rounded-2xl border border-border bg-white shadow-xl"><div className="flex items-center justify-between border-b border-border px-4 py-3"><div><p className="text-[11px] font-black uppercase tracking-wider text-primary">Cập nhật cước COD/CC</p><h2 className="text-base font-black text-foreground">{waybill.waybill_code}</h2></div><button onClick={onClose} className="rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground"><X size={18} /></button></div><div className="space-y-3 p-4"><label className="block text-[12px] font-bold uppercase tracking-wider text-muted-foreground">Cước phí cost_amount</label><input value={formState.cost_amount} onChange={event => onChange(event.target.value)} inputMode="decimal" className="h-10 w-full rounded-xl border border-border bg-white px-3 text-[13px] font-bold outline-none focus:border-primary" placeholder="Nhập cước phí" />{error ? <div className="rounded-xl border border-red-200 bg-red-50 px-3 py-2 text-[13px] font-bold text-red-700">{error}</div> : null}<div className="flex justify-end gap-2 pt-1"><button onClick={onClose} className="h-9 rounded-lg border border-border bg-white px-3 text-[13px] font-bold text-muted-foreground hover:bg-muted">Hủy</button><button onClick={onSubmit} disabled={isSubmitting} className="inline-flex h-9 items-center gap-2 rounded-lg bg-primary px-3 text-[13px] font-bold text-primary-foreground disabled:opacity-60">{isSubmitting ? <Loader2 size={15} className="animate-spin" /> : null} Lưu</button></div></div></div></div>;
}
