import { Loader2, X } from 'lucide-react';
import type { AssignDriverHandoverFormState, UserSummary, WaybillHandoverItem } from '../types';

interface Props { isOpen: boolean; waybill: WaybillHandoverItem | null; driver: UserSummary | null; formState: AssignDriverHandoverFormState; error: string; isSubmitting: boolean; onConfirm: () => void; onBack: () => void; onClose: () => void }
export default function HandoverConfirmDialog({ isOpen, waybill, driver, formState, error, isSubmitting, onConfirm, onBack, onClose }: Props) {
  if (!isOpen || !waybill) return null;
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4"><div className="w-full max-w-md overflow-hidden rounded-2xl bg-white shadow-xl"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><p className="text-[12px] font-bold uppercase tracking-wider text-primary">Xác nhận bàn giao</p><h2 className="text-lg font-black text-foreground">{waybill.waybill_code}</h2></div><button onClick={onClose} className="flex h-9 w-9 items-center justify-center rounded-lg hover:bg-muted"><X size={18} /></button></div><div className="space-y-4 p-5">{error && <div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-[13px] font-bold text-red-700">{error}</div>}<div className="rounded-xl border border-border bg-muted/10 p-4 text-[13px] leading-6"><p><b>Tài xế:</b> {driver?.name || driver?.username || `#${formState.driver_id}`}</p><p><b>Chuyến xe:</b> {formState.trip_id ? `#${formState.trip_id}` : 'Không gắn chuyến'}</p><p><b>Trạng thái sau bàn giao:</b> OUT_FOR_DELIVERY</p></div><div className="flex justify-end gap-2"><button onClick={onBack} className="h-10 rounded-lg border border-border px-4 text-[13px] font-bold text-muted-foreground hover:bg-muted">Quay lại</button><button onClick={onConfirm} disabled={isSubmitting} className="flex h-10 items-center gap-2 rounded-lg bg-primary px-4 text-[13px] font-bold text-white hover:bg-primary/90 disabled:opacity-60">{isSubmitting && <Loader2 size={15} className="animate-spin" />} Xác nhận</button></div></div></div></div>;
}
